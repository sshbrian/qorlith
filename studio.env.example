# Optional. Ports in qorlith.yaml win when start-monitor runs.
# Copy to studio.env (gitignored) only if you need shell overrides.

QORLITH_ROOT=
COMFY_URL=http://127.0.0.1:8188
COMFY_ROOT=
COMFY_OUTPUT=
QORLITH_PORT=
QORLITH_WEB_PORT=

# Hugging Face. Default is ~/.hf-token/token (never commit the token).
# HF_TOKEN_FILE=/home/neo/.hf-token/token
